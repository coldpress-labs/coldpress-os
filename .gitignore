# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Deprecated agent personas (excluded from repo)
agents/_archive/

# Environment
.env
.env.local
.env.*.local

# OS
Thumbs.db
Desktop.ini

# Node / npm package build
node_modules/
dist/
*.tsbuildinfo
npm-debug.log*
.npm
coverage/
.vitest-cache/

# Packed tarballs (never commit; `npm pack` output — see overhaul WS0 §8 item 3)
*.tgz

# Runtime state — EventStream run-logs, graph index, local-config (never tracked;
# tests + live CLI runs write here in the repo cwd)
.coldpress/
