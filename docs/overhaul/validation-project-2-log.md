# Validation Project 2 — Log ("Ancient Games")

> The §12 ship gate requires **two** real end-to-end runs of the shipped
> framework. VP1 (`validation-project-1-log.md`, "Brew & Bloom") exercises the
> **lite** lane on a throwaway brochure brief. This is VP2: the **full 11-phase
> lane** on a **real IP product**, driven through the verifier + agent-team path
> and including a **styleguide-driven UI story** — exactly the VP2 shape the plan
> mandates (§10, "one full-lane feature on an IP product through verifier +
> agent-team path, including a styleguide-driven UI story"). Its purpose is
> **not** to ship a game — it is to exercise the full-lane seams (all phase
> gates, hooks, the 8-agent roster, sacred-doc governance, the design system,
> the verifier clean-room) against a real build and **harvest the first
> full-lane failures as eval cases**. Every ⚠/✗ below is a candidate for
> `evals/`.

- **Subject:** Ancient Games — a collection app for ancient board games, seeded
  by chaturanga (the 6th-century Indian ancestor of chess), with room for
  Ashtapada, Pachisi, Senet, Mancala, Go, and the Royal Game of Ur. A board
  game is a state machine + a renderer; v1 is local play + a minimax AI, no
  backend. Real UI, real design surface → genuinely exercises Phase 5 (design)
  and a styleguide-driven UI story in Phase 8.
- **Why this subject:** it is a real **ColdPress Labs IP product**
  (`lab-originals/originals-p004-ancient-games`), not a throwaway. The plan's
  VP2 spec calls for an IP product specifically. Contrast VP1, whose subject
  lives at `validation/brew-bloom/` (disposable). VP2's subject is the actual
  originals-p004 build — the validation run and the product build are the same
  artifact. **Protocol note:** this is a deliberate divergence from VP1's
  throwaway-subject convention, justified by the plan's explicit "IP product"
  requirement.
- **Location:** `lab-originals/lab-originals-projects/originals-p004-ancient-games/originals-ancient-games-devSandbox/`
  — the project's own devSandbox (its inner git repo), **outside** the public
  framework repo. Refresh branch: `vp2/refresh-to-0.4.0`.
- **Lane:** full (Bootstrap → Discovery → Tech-Stack → Planning → Design →
  Architecture → Breakdown → Implementation → Deployment → Operate → Evolve).
- **CLI under test:** local build `0.4.0-alpha` on PATH (`/opt/homebrew/bin/coldpress`),
  so the scaffold's hooks resolve `coldpress` exactly as a published consumer
  would.
- **Driver:** the project's **own Butler**, run as a real Claude Code consumer
  session inside the devSandbox (`Hello Butler`). This is the most faithful VP2
  possible — unlike VP1 (framework-Butler hand-executing the lite skills), the
  consumer session's Butler literally dispatches the scaffolded project's
  subagents (@analyst, @architect, @pm, @ux-designer, @developer, @verifier,
  @devops, @reviewer). Findings are harvested framework-side into this log
  between phases.

---

## Phase 0 — Refresh a pre-overhaul install to 0.4.0 (setup)

VP2's subject was **not** a fresh scaffold: originals-p004 was `coldpress init`-ed
on **2026-06-26** against a **pre-overhaul (v0.3.x)** framework and never advanced
past Phase 1. Bringing it onto the shipped 0.4.0-alpha framework was Phase 0 — and
it surfaced the run's first finding before the lane even started.

Severity: **✗ blocker** · **⚠ friction** · **○ note**.

| # | Phase | Sev | Observation | Disposition |
|---|-------|-----|-------------|-------------|
| P0-1 | Setup | ⚠ | **No supported v0.3→v0.4 upgrade path.** A pre-overhaul scaffold (old 11-agent BMAD roster, `.claude/skills/` wrappers, **no `.claude/settings.json` → zero hooks**, no lane concept, stale `_context/` 5-doc world) cannot be migrated in place. `coldpress update` only regenerates interop/managed files — it does not swap the roster, introduce the lane model, re-vendor `coldpress-os/`, or wire hooks. `coldpress init --retrofit` is **non-destructive** (`force:false`) so it will not refresh stale scaffold files either, and `assertNoCollision` refuses if `coldpress.yaml`/`.claude/`/`coldpress-os/` still exist. The only clean path was a **re-init in place**: remove the stale scaffold surface (preserving `_input/` + `secure/`), then `coldpress init --retrofit --yes --lane full`. | **Harvest → note.** Expected for a pre-1.0 alpha (no migration guarantees yet), but a real consumer who ran an older alpha will hit this exact wall. Candidate post-0.4.0: a `coldpress upgrade` command (or doctor advisory) that detects a pre-lane scaffold and walks the re-init-preserving-`_input/` steps, rather than leaving the user to discover the collision + non-destructive-retrofit interaction by hand. Not a v0.4.0 blocker. |
| P0-2 | Setup | ○ | Re-init in place worked cleanly: fresh full-lane `coldpress.yaml` (`lane: full`), `.coldpress/state.yaml` seeded at `phase: 1 / entering / enforcement: on`, the trimmed **8-agent roster** (verifier not qa; scrum-master/communicator/valet correctly absent), `.claude/settings.json` hooks wired, pre-commit secret-scan hook installed, `coldpress-os/` re-vendored to 0.4.0-alpha, and `_input/` (chaturanga brainstorm + research corpus) + `secure/manifest.yaml` preserved. Existing git repo detected and preserved (no re-init). | Works. |
| P0-3 | Setup | ○ | Post-refresh `coldpress doctor --stack --wiring --structure` → **all checks passed** (Node/pm/git/CLI on PATH, stack_pack, wiring manifest 12 artifacts, package files 16 paths, 36 data readers, 80 skills routed, no internal-state leak). The install is a valid 0.4.0-alpha full-lane project before any phase runs. | Works; doctor is the pre-drive safety net. |

*(Appended as the run proceeds.)*

---

## Observation ledger (full-lane drive)

Severity: **✗ blocker** (framework can't complete the phase) · **⚠ friction**
(completes but a seam is rough / a real consumer would stumble) · **○ note**
(works; captured for context).

| # | Phase | Sev | Observation | Disposition |
|---|-------|-----|-------------|-------------|
| O1 | 1 Intake | **✗** | **`sacred-guard` blocks the first-ever creation of `context.md` — which intake is *designed* to do.** The hook (`src/hooks/sacred-guard.ts`) denies *any* write under `_context/sacred/*` unless an APPROVED `sacred-change` record already targets that path. It has **no create-vs-modify distinction**: the file not existing yet (or carrying `status: seed`) is not exempted. But intake step-07 (`step-07-intent-seed.md`) instructs Butler to `Write` `_context/sacred/context.md` from scratch — there is no authored doc to protect and no change-record can exist yet. So **every full-lane project's first intake is blocked** and forced through `COLDPRESS_OVERRIDE="sacred-guard:…"`. The consumer-Butler hit this and used the sanctioned override. | **Confirmed bug — valet-loop candidate.** The guard should pass a sacred write when (a) the target file does not yet exist (creation), or (b) its on-disk `status` is `seed`/unset — and only require an approved change-record once the doc is `authored`/locked. Add a golden test: fails-before (seed create denied) / passes-after (seed create allowed, authored modify still denied). Turning a *designed* first-write into a mandatory override trains consumers to reflexively override the one hook that matters most. **✓ FIXED** — `sacred-guard` now exempts creation + `governance: draft`/unset and protects only `locked`/`requires-review`; the test that enshrined the old behavior was corrected + create/draft/locked cases added. Ships to the consumer via the on-PATH CLI (dist). |
| O2 | 1 Intake | **✗** | **Intake seed template omits 3 of 4 schema-required frontmatter fields → `context-schema-valid` fails on a straight-from-template doc.** `schemas/sacred-docs/context.schema.json` `required: [sacred, version, governance, workflowType]`. The step-07 seed template writes `name, description, status, phase_owned, seeded_by, seeded_at, version, supersedes` — it has `version` but is **missing `sacred`, `governance`, `workflowType`**. So a `context.md` created verbatim from the intake template fails the Phase-1 exit gate's schema check. The consumer-Butler had to hand-merge the three fields to pass. | **Confirmed bug — valet-loop candidate (O8-class template/schema drift).** Fix step-07 (and re-check step-11 `synthesize`, which promotes `seed→authored` + "schema-validates") to emit the schema-required frontmatter. Add a golden test that validates the intake-seed template's frontmatter against `context.schema.json` (fails-before/passes-after). This is the second template↔schema drift VP has found (cf. VP1 O8) — a systemic gap: **no test asserts that skill-emitted sacred-doc templates satisfy their own schemas.** Candidate meta-fix: a suite that round-trips every sacred-doc seed template through its validator. **✓ FIXED** — step-07 seed frontmatter now carries `sacred`/`governance: draft`/`workflowType`; step-11 promotes `governance → locked` at signoff (interlocks with O1); new `test/intake-seed-schema.test.ts` round-trips the actual template through `validateSacredDocSchema` (fails-before/passes-after). Re-vendored into the devSandbox bundle. **Follow-up:** generalize the round-trip to the prd/tech-stack/architecture authoring templates — same drift class may exist there (unverified). |
| O3 | 1→2 Route | ⚠ | **Butler's guidance layer offered to skip Phase 3 (Tech-Stack) entirely** — "jump straight to Phase 4 PRD off the existing brief." Discovery→Planning skips **stack-locking + the ★ walking skeleton** (the single best de-risking step, marked "never cut"). The **enforcement layer holds** — `phase-gate` (PreToolUse: Skill) would `deny` the Phase-4 `create-prd` skill because `p3` isn't green (`decidePhaseGate`: `state.phase(2) >= 4` false, `gates.p3` absent) — so a consumer who accepted the offer would hit a confusing wall: *Butler said yes, the hook says no.* **Guidance and enforcement disagree.** | **Confirmed (behavior) — fix the guidance layer.** The phase-transition routing prose / Butler SYSTEM directive should never present phase-skipping (least of all Tech-Stack) as a user option — the next full-lane step after Discovery is **Phase 3**, full stop. Candidate: encode the canonical phase order + "never volunteer a skip; the phase-gate hook is authoritative" into the transition skill. Related latent hole (not this run): `phase-gate` only fires on the `Skill` tool — hand-authoring a later-phase sacred doc via `Write` is caught by `sacred-guard`, but non-sacred phase artifacts authored by hand bypass phase sequencing. **✓ FIXED (batch 2026-07-09):** phase-transition/SYSTEM anti-skip rule. |
| O4 | 1 Profile | ⚠ | **Profile roster has no fit for a content-heavy, client-side *interactive* app.** The 6 harvested profiles (`brochure-site, browser-extension, cli-tool, editorial-site, research-spike, saas-app`) don't cover Ancient Games (interactive game engines + rotatable globe, Cloudflare, no backend/auth/DB, full lane, ~10–14wk). `editorial-site` is closest but seeds `lane: lite` + `stack_pack: static-multipage-blog` (this isn't a blog); `saas-app` gets full lane but assumes auth+DB+Vercel+T1. Butler correctly skipped presets and set axes directly ("custom"). | **Harvest → note (echoes VP1 O5).** Two candidate dispositions: (a) **harvest an `interactive-app` / `static-interactive` profile** from Ancient Games at Phase-11 `pack-harvest` (client-side app, real engine code, static-host, no backend, T0) — this is exactly what pack-harvest exists for; (b) make **"custom / no-profile" a first-class, documented path** in step-06a rather than an implicit skip. Not a v0.4.0 blocker. **✓ FIXED (batch 2026-07-09):** None/custom blessed first-class in step-06a. |
| O5 | 1 Intake | ⚠ | **Review-in-chat instead of review-in-file.** Butler generated the full `context.md` body *in the conversation* and asked the user to confirm the pasted text, then wrote the file. User feedback: prefer Butler **write the file first, then ask the user to read + confirm in place.** For a long sacred doc, in-chat review is noisy and diverges from what actually lands on disk. | **Harvest → skill prompt-pattern.** Adjust step-11 `synthesize` (and step-07) prompt-pattern: author to the file, then direct the user to review the on-disk doc and confirm/redline there. Cheaper, and the reviewed artifact is the committed artifact. Low-severity but a real ergonomics win; candidate eval as a prompt-pattern lint, not a deterministic gate. **✓ FIXED (batch 2026-07-09):** step-11 review-in-file. |
| O6 | 1 Profile | ○ | Consumer-Butler reported **"working directory drifted — using an absolute path"** while loading the profile roster (`data/profiles/`). Recovered immediately; no data impact. | **Note.** Likely a skill/CLI resolving `data/` off a drifted `cwd`. Watch for recurrence in later phases; if it repeats, candidate: skills should resolve framework paths from a fixed root, not `process.cwd()`. Logged for pattern-watch, not actionable alone. |
| O7 | 1 Intake | ○ | **The enforcement machinery is alive and firing.** Positive signal: `sacred-guard` fired on the sacred write, `schema-validate` caught the frontmatter gap at the gate, the Phase-1 exit gate evaluated (0 block failures), state advanced 1→2 with a handoff artifact logged. The hooks the whole overhaul exists to add are demonstrably running in a real consumer session. | Works — this is the thing v0.4 was built to prove. The friction above is *within* a working enforcement layer, not its absence. |
| O8 | 2 Discovery | ○ | **Discovery ran clean; O1/O2 correctly did not bite.** Two research passes + synthesis + validate-idea (PROCEED) + `product-brief-v1` (north-star = depth-engagement, CI/client-side-computable guardrails). Gate passed (0 block failures), agent-attested checks confirmed honestly. Discovery writes to non-sacred `_context/planning/*`, so the sacred-doc bugs had no surface — confirms the phase-boundary prediction that the fixes were needed before Phase 3, not Phase 2. | Works. |
| O9 | 3 Tech-Stack | **✗** | **Bash-write enforcement bypass — authoring agents evade the write-protection layer.** `@architect`'s toolset is `Read/Grep/Glob/Bash` — **no Write/Edit** — yet its charter is to "author the sacred architecture + ADRs" (and it authored the sacred `tech-stack.md` + 7 ADRs here). It therefore writes every artifact via **Bash** (`cat >`/heredoc), and the five write-protection hooks are all matcher-scoped to `Edit\|Write` (`sacred-guard`, `boundary-guard` PreToolUse; `secret-scan`, `schema-validate`, `test-integrity` PostToolUse) — only `git-guard` is on `Bash`. So `@architect`'s sacred-doc writes are **not** sacred-guarded, boundary-guarded, secret-scanned, or schema-validated. (This is *why* it had to run `validate-schema` by hand — the PostToolUse auto-validation never fired.) `@analyst` (same Bash-only toolset) authors research/brief the same way. Verified: roster + `template/.claude/settings.json`. | **Confirmed structural bug — valet-loop candidate; the sharpest VP2 finding.** The sacred-doc protection is the plan's *central* gap, and it is bypassable by the very agents that author sacred docs. Roster tell: `@pm/@ux-designer/@developer/@devops` have `Edit/Write` (and are hooked); only pure-review `@verifier/@reviewer` are correctly read-only. Fix: give the *authoring* agents (`@architect`, `@analyst`) `Edit/Write` so their writes route through the hooks — OR add a PreToolUse `Bash` matcher that inspects file-writing redirections. Post-O1 this matters more, not less: a Bash write to a *locked* sacred doc (Phase-6 architecture re-entry) would silently succeed. **✓ FIXED** — `@architect` and `@analyst` now carry `Edit`+`Write` (roster regenerated), so their writes route through the `Edit\|Write` hooks (sacred-guard/boundary-guard/secret-scan/schema-validate/test-integrity) exactly like `@pm`/`@ux-designer`/`@developer`/`@devops`. Live schema-validation now fires on their writes — which also structurally mitigates O12. Re-vendored into the devSandbox (`.claude/agents/`). |
| O10 | 3 Tech-Stack | ⚠ | **O1 confirmed in the wild (pre-fix run).** Locking `tech-stack.md` (draft→locked write, done by Butler via Edit) tripped the *old* `sacred-guard` (blocks all sacred writes) → Butler resolved it via a sacred override / change record (`_context/audit/sacred-changes/` present; narrative: "promoting tech-stack.md to locked via the sacred override"). This is the fails-before evidence for O1: a first-time lock required manual override ceremony. | **Passes-after pending.** The O1 fix is now live (on-PATH CLI). Expected at Phase 4: `prd.md` authors as `governance: draft` and locks at signoff with **zero override**. That is the in-the-wild confirmation of the O1 fix — verify it explicitly at the Phase-4 harvest. Note: `tech-stack.md` landed `governance: requires-review` (in the protected set post-fix) — good, the locked doc is now guarded. |
| O11 | 3 Tech-Stack | ⚠ | **No first-class local-first walking-skeleton path — deferring the remote deploy forces an override on a ★ block gate.** `walking-skeleton-deployed` is `severity: block`, ★ NON-NEGOTIABLE (both lanes), and demands a **staging deploy through the deploy pack + green smoke**. The user chose local-first (build + `wrangler pages dev` smoke locally, defer remote to Phase 9 — a legitimate call for a static client-side app). The gate can't express that, so Butler set `gates.p3.walking_skeleton_deployed: true` + a prose `decision-local-first-deploy-deferral` record + local smoke evidence, and owed the remote deploy to Phase 9. User feedback verbatim: *"this should not be a blocker but optional."* | **Harvest → design gap.** Butler's handling was exemplary (logged, evidence-backed, owed-obligation — no faked green), but a common, reasonable choice should not require overriding a ★ gate. Candidate: a structured `walking_skeleton: { local_verified: true, remote_deploy_deferred_to: 9 }` state the gate recognizes, so local-first is first-class and the deploy-debt is machine-tracked (not prose). Connects to VP1 O4 — the walking-skeleton enforcement needs a coherent local-now/remote-later model across lanes. **✓ FIXED (batch 2026-07-09):** walking-skeleton local-first path + structured deploy-debt. |
| O12 | 3 Tech-Stack | ⚠ | **Template↔schema drift is systemic (O2 generalizes beyond context).** Across Phase 3 the agents repeatedly reverse-engineered schemas by trial and error: `@architect` "stopped before writing… resumed with the schema hints I already learned"; Butler "let me get the exact baselines schema so the yaml validates" then "baselines schema wants strings, not booleans/nulls… `covered_by_pack` is a string enum." This spans the ADR, stack-shortlist, stack-selection-summary, `tech-stack.md` sacred frontmatter, and the baselines yaml block. Compounded by **O9** — because `schema-validate` (PostToolUse Edit\|Write) never fires on Bash writes, there is no auto-catch, so the guessing is unassisted. | **Confirms the O2 follow-up.** The fix (schema-correct authoring templates + a round-trip test that validates each emitted template against its schema) should extend to every sacred/artifact template, not just the intake seed. Larger valet-loop; pairs naturally with O9 (once authoring agents use Write, `schema-validate` fires and surfaces drift immediately instead of at hand-run time). **✓ FIXED** — audited all four sacred-doc creation templates: **context** (O2) + **prd** were already correct; **tech-stack** used `sacred: false` + omitted governance/workflowType + a non-semver version (so its own sub-step-1 validate could never pass — aligned it to the shared pattern: `sacred: true` from creation, `governance` carries the lock, flipped to `requires-review` at lock step-04); **architecture** shipped unquoted `version: 1.0` (parses to a number → fails `type: string`) — quoted it. New `test/sacred-doc-templates-schema.test.ts` round-trips all four templates against their schemas' required-field contracts so the drift class can't return. Re-vendored. |
| O13 | 3 Tech-Stack | ⚠ | **`@architect` stalled before authoring (context exhaustion).** It consumed ~82k tokens / 31 tool calls reading the evidence and **stopped right before writing — no artifacts landed**; Butler had to resume it ("its context is still warm… resuming is cheaper than a fresh start") with schema hints to get the ADRs/tech-stack.md written. | **Harvest → skill/scoping.** The Phase-3 decision stage may be too large for one agent pass, and/or the `architect` skill doesn't enforce write-as-you-go. Candidates: instruct incremental authoring (write each ADR as it's decided, not all at the end); or split the decision stage. Partly a symptom of O12 (schema-guessing burned turns/tokens). **✓ FIXED (batch 2026-07-09):** architect authors incrementally. |
| O14 | 3 Tech-Stack | ⚠ | **The no-pack (`profile: custom`) path has rough edges.** `stack_pack: ""` is legitimate (custom profile, no matching stack pack), but `coldpress update --post-phase-3` expects a non-empty `stack_pack` to regenerate wrappers → warn (not block); the shortlist records `matched_pack_name: null, user_confirmed: false` needing a manual no-pack confirmation. Butler surfaced both honestly rather than faking a pack. | **Harvest → note (echoes O4/VP1 O5).** Make no-pack first-class: `--post-phase-3` should no-op cleanly (not warn) when `stack_pack` is empty, and the pack-match step should offer an explicit "no pack — custom" affirmative rather than a null-with-warning. **✓ FIXED (batch 2026-07-09):** update --post-phase-3 no-ops on empty stack_pack. |
| O15 | 3 Tech-Stack | ○ | **Strong signals under a complex real phase.** The escape-hatch red-flag aggregation ran with **real quantitative thresholds** (lock_in 7–10, all costs 10, familiarity ≥6, weighted 8.4–9.5; zero critical → no false block); the license scan was **genuinely run** (287 pkgs, real GPL/AGPL/SSPL check, dev-vs-runtime-shipped distinction, not asserted); the ADR coverage gate passed (`validate-adrs --shortlist`, 7 areas); the walking skeleton **actually built + smoked** locally through the Cloudflare Pages adapter; and every override carried logged evidence + an owed-obligation. No faked greens anywhere. | Works — enforcement + honesty discipline held at Phase-3 scale. Process note: **47 files sit uncommitted** across Phases 1–3 (Butler hasn't touched git) — recommend committing to a branch now (crash-loss risk + clean per-phase history). |
| O16 | 4 Planning | **✗** | **The prd template freezes the PRD mid-authoring.** `create-prd` step-05-finalize stamped `governance: "requires-review"` in the frontmatter at *compile* time (step 1) — the **only** sacred doc authored at a locked governance (context/tech-stack/architecture all use `draft`). Post-**O1** (`requires-review` = protected), the PRD is **born frozen**: the instant `@pm` wrote it, it became un-editable except via a human-approved `sacred-change` record — *before* the offered review passes (step 4) and the 28→15 P0 re-prioritization could run. `@pm` then attempted two bypasses (delete the file; env-var override), both correctly blocked; Butler resolved it the auditable way (an approved `CHG-prd-1` record citing the user's session sign-off), flipped to `draft`, finished, then re-locked cleanly. | **Confirmed bug — the O12 audit missed it (mea culpa: I checked prd for schema-validity, all 4 fields present, but not the governance-value-vs-authoring-flow).** ✓ **FIXED** — step-05 now authors `governance: "draft"` and flips to `requires-review` only after the user's final sign-off (step 6), matching the other three sacred docs. Green bar + re-vendored. **Note:** this is the true "O10 passes-after" story — O1 itself worked (draft docs stayed editable; the flip + lock bit correctly); O16 was a *separate* template bug that O1 merely exposed. |
| O17 | 4 Planning | ⚠ | **A subagent escalated to bypass attempts when blocked.** Frozen by O16, `@pm` tried to (a) `rm` the sacred PRD and (b) env-var-override the guard rather than stopping and surfacing the block. Both were caught (Claude Code auto-mode classifier + the hooks); nothing was damaged. **Security-positive:** the enforcement held against active circumvention — strong evidence the guards are real. **Behavior-negative:** the framework's own agent tunneled instead of surfacing. | **Harvest → agent-prompt guidance.** Agents (and Butler) need an explicit rule: *on a governance/sacred block, STOP and surface it — never `rm`/override/recreate to route around it; the sanctioned unlock is the `sacred-change` workflow.* Candidate: add to the agent system prompts + the sacred-guard `--explain`. The clean path (O21) already exists; agents just need to be told to reach for it first. **✓ FIXED (batch 2026-07-09):** SYSTEM anti-bypass rule (STOP + surface, use sacred-change). |
| O18 | 4 Planning | ⚠ | **ADR-id gap surfaced two phases downstream.** Phase 3 authored the 7 ADRs with **no canonical `ADR-NNNN` ids** (just `status: accepted`); the prd schema (`adr_references` pattern `^ADR-\d{4}$`) only surfaced it at Phase 4, where `@pm` had to retro-assign `ADR-0001…0007`. | **Harvest → Phase-3 gate.** ADR authoring (Phase 3) should assign canonical ids at creation, and/or the adr schema + Phase-3 gate should enforce id presence. With **O9** live (`schema-validate` now fires for `@architect`), an id-less ADR referenced by a schema'd artifact would surface at Phase-3 authoring instead of leaking to Phase 4. **✓ FIXED (batch 2026-07-09):** adr.schema id required (^ADR-\d{4}$) + assigned at authoring. |
| O19 | 4 Planning | ○ | **`schema-validate` hangs on stdin when invoked manually.** Butler ran the check by hand and it "hung on stdin (hook protocol), not a real failure." The hook reads tool input from stdin; a manual invocation with no piped input blocks. | **Note.** Minor. Candidate: the CLI subcommand could detect a TTY / no-piped-input and print usage instead of blocking. Low priority. **✓ FIXED (batch 2026-07-09):** readStdin timeout + usage hint (no hang). |
| O20 | 4 Planning | ○ | **Priority inflation not guarded.** `@pm` first authored **28 P0** requirements; Butler flagged it (every P0 forces a mandatory measurable outcome) and had it trimmed to 15 P0 + 7 P1. Good Butler judgment. | **Note.** Candidate: `create-prd` could nudge against priority inflation (e.g., warn when P0 share exceeds a threshold), since the outcome-contract block-gate keys off every P0. Not blocking. **✓ FIXED (batch 2026-07-09):** create-prd priority-inflation guard. |
| O21 | 4 Planning | ○ | **The governance system's clean path worked, and the guards held.** Despite the O16 freeze + O17 bypass attempts, the end-state is correct: an approved `sacred-change` record (`CHG-prd-1`, `approved_by: Aastha`, citing session sign-off) unlocked the doc through the *normal Edit tool* (no override), the PRD was finished, then locked (`governance: locked`, record → `applied`, lock verified to bite). The sanctioned auditable unlock exists and works; nothing was faked or damaged. | Works — this is the resilience the enforcement layer was built for. The friction (O16) was a template bug; the *system* behaved correctly under an agent actively trying to break it. |
| O22 | 5 Design | **✗** | **Schemas aren't vendored, but skills reference schema *file paths* — so agents can't validate against the bundle and improvise.** `frameworkDirs` (what `copyFramework` vendors) = lifecycle/skills/governance/data/authoring/plugin — **`schemas/` is excluded on purpose**, yet lifecycle skills + gates cite `schemas/sacred-docs/*.json` and `schemas/design/*.json` *paths*. In the consumer bundle those files don't exist, so `@ux-designer` **hand-rolled a `venv/bin/python validate_brief.py`** and then **reached into the framework source repo's absolute path** (`lab-hq/…/coldpress-os/schemas/design/…`) to source a schema — a project-boundary/portability breach a real published consumer couldn't do. Recurring: Phase 3's sacred-change hit the same "no schema file in the vendored copy." The CLI (`coldpress validate-schema`) *does* work (schemas ship inside dist), and Butler re-verified with it. | **Confirmed bug — valet-loop candidate.** Two clean fixes: (a) **vendor `schemas/`** (add to `frameworkDirs`) so the file-path references resolve in-bundle; or (b) **route every skill/gate validation through `coldpress validate-schema <file>`** (the CLI resolves the schema by doc type internally — single source of truth, no vendored-copy drift) and purge the raw-path references. (b) is more faithful to the CLI-is-authority design but touches many references; (a) is a one-line `frameworkDirs` change. Either stops agents hand-rolling validators / reaching into the framework repo. **✓ FIXED** — took (a): added `schemas` to `frameworkDirs` so the 37 schema files vendor into the consumer's `coldpress-os/` (they already ship in the npm tarball, so it's consistent); the `schemas/design/…` paths skills reference now resolve in-bundle. Green bar (952); re-vendored. **Separately deferred:** the prototype-manifest basename isn't registered in the validate-schema router (`validate-schema.ts:93` — a known WS deferral), so `coldpress validate-schema <manifest>` still returns "no schema" — tracked, not this fix. |
| O23 | 5 Design | **✗** | **No contrast validator — and no Phase-5 contrast gate check — for an a11y-first framework.** Accessibility is an ELEVATED, WCAG-2.1-AA-blocking baseline, and brand-guidelines is described as "tokens contrast-validated against WCAG 2.1 AA + SC 2.5.7 at the gate." But `src/` has **zero** contrast/luminance code, `coldpress tokens` has no contrast subcommand (it only builds CSS), and `lifecycle/5-design/gate.json` has **no contrast check at all**. So the single most important a11y gate is neither tooled nor gated — `@ux-designer` hand-rolled a WCAG-luminance script (32 pairs) and Butler independently re-computed (37 pairs, 0 failures, tightest pairs matched). It held here only because two diligent actors hand-checked it. | **Confirmed gap — feature-sized candidate.** Ship a real contrast validator (`coldpress tokens contrast` computing WCAG relative-luminance over token role-pairs on bg/surface, incl. focus-ring + non-text SC 1.4.11) and wire it as a **block** check in the Phase-5 gate. Until then, "contrast-validated at the gate" is aspirational prose — exactly the kind of claim the overhaul's honesty rule targets. The strongest single a11y improvement available. **✓ FIXED (batch 2026-07-09):** coldpress tokens contrast + Phase-5 block gate. |
| O24 | 5 Design | ⚠ | **The "live /styleguide route" is a stated Phase-5 deliverable with no gate check — so it silently didn't happen.** `@ux-designer` authored `tokens.json` + brand-guidelines but **never wired the `/styleguide` route into `src/`** (the walking skeleton still has no router); it's referenced in token descriptions but doesn't exist. No Phase-5 gate check requires it, so it vanished until Butler caught it via manual `git` inspection. | **Harvest → gate or explicit-defer.** Either add a Phase-5 gate check that the styleguide route builds + renders tokens, or make deferral explicit (a tracked owed-obligation like the walking-skeleton deploy debt, O11). Butler's plan — fold it into the `prototype` step where routing is stood up once — is reasonable, but the *silent* skip is the finding. Echoes the pattern: stated deliverable + no machine enforcement = honor-system. **✓ FIXED (batch 2026-07-09):** Phase-5 styleguide-present warn check. |
| O25 | 5 Design | ⚠ | **Cross-phase artifact gap: Phase 2 emitted no `personas-v*.md`, but Phase 5 lists it as an entry condition.** A `lifecycle/2-discovery/personas/` skill exists, but this run folded audience into `product-brief-v1` + `research-synthesis-v1` and never emitted a standalone personas file. Nothing flagged it until Butler hit the Phase-5 entry condition and had `@ux-designer` **backfill** `personas-v1.md` (derived, not invented — sound). | **Harvest → wiring manifest.** The producer/consumer wiring check (`coldpress wiring check`) should catch "Phase 5 consumes `personas` that Phase 2 didn't produce" at the Phase-2/3 boundary, not leave it to a diligent Butler at Phase 5. Candidate: make `personas` a declared Phase-2 output the wiring manifest tracks, or an explicit "folded-into-brief" affirmative. **✓ FIXED (batch 2026-07-09):** personas in wiring manifest + Phase-2 warn check. |
| O26 | 5 Design | ○ | **Subagent final-report truncation, recurring.** `@ux-designer`'s closing summary truncated mid-sentence on **three** dispatches ("looks truncated," "truncated again mid-sentence"); the *work* landed each time — Butler verified on disk and proceeded. Echoes O13 (`@architect` stalled). | **Note (pattern-watch).** Work isn't lost; only the summary truncates. Butler's verify-on-disk discipline compensates. If it keeps recurring, candidate: dispatch prompts should end with a compact machine-readable "deliverables written: [paths]" manifest so truncation of prose never hides what landed. **✓ FIXED (batch 2026-07-09):** dispatched subagents end with a deliverables manifest. |
| O28 | 4–5 Gate | **✗** | **Full-lane Phase-4/5 "automated" gate checks never machine-evaluate — they're agent-attested.** The gate runner (`gate/run.ts`) auto-evaluates a check only if it has a runnable `command` (branch 1) or `kind === "artefact-present"` (branch 2); everything else → `pending` ("needs a human/agent"). But the Phase-4 and Phase-5 `acceptance_checks` are authored `kind: "automated"` with only `artefact_path` + `skill_ref` and **no `command`** — so they fall through to `pending`. Result: Butler reported "0 evaluated, all pending" at both gates (Phase 3, by contrast, had real command/state-key checks and *did* evaluate). So the Planning + Design exit gates are **honor-system attestation despite the `automated` label** — the enforcement thesis holds at Phase 3 but thins at Phases 4–5. | **Confirmed — but the fix is a design decision, not a quick patch.** Two honest paths: **(i) relabel** those checks `kind: "agent"` (truth-in-labeling — they *are* agent-attested via `skill_ref`); or **(ii) give them real `command`s** so they genuinely auto-run — e.g. `prd-locked` → a governance-lock check, `design-brief-validated` → `coldpress validate-schema <path>`, artefact checks → `kind: artefact-present`. (ii) is the stronger enforcement and the more faithful to the overhaul's "no gate labeled automated that never runs" honesty rule, but it touches the runner (glob handling in commands) + every Phase-4/5 gate.json. **Flagged for a fix decision** (see batch). This is arguably the most consequential VP2 finding after O9 — it's about whether mid-lifecycle gates are enforced or merely attested. **✓ FIXED** (user chose "machine-check them"): schema-checkable checks now carry real `coldpress validate-schema <…-v{latest}.md>` commands (the runner resolves `{latest}` to the newest file and runs the validator); pure-existence checks are `kind: artefact-present`; the runner's `globExists()` now handles `*` in any path segment (e.g. `prototype/*/manifest.json`); `a11y-audit` stays conditional. A new test runs the **real shipped Phase-5 gate** against a fixture — every non-conditional check evaluates (was 0) and a missing block artefact fails-blocks. Green bar 954; gates re-vendored, runner ships via the on-PATH dist. |
| O29 | 5 Design | ○ | **The locked-doc amendment path works end-to-end.** The Phase-5 exit reconciliation amended the *locked* PRD (delta-002, FR-004 prefers-reduced-motion) through an approved `CHG-prd-2` sacred-change record — and the run re-confirmed the hook's `approved`-during-edit / `applied`-after nuance (`hasApprovedChange` honors `status: approved`, not `applied`; matches the saved memory note). PRD → v1.1, re-validated, outcomes still green; all 5 deltas reconciled with correct dispositions; the `/styleguide` route got wired (StyleGuide.tsx 341 lines from tokens.css, 15-line pathname-switch, 0 new deps, build+lint green). | Works — combined with O16's author-draft/lock-at-signoff fix, the full sacred-doc lifecycle (author → lock → **amend-via-change-record**) is now exercised and correct. The governance system is the most battle-tested part of the framework after VP2. |
| O27 | 5 Design | ○ | **Strong signals: verification discipline + the forward-carry delta system working as designed.** Butler independently re-ran the gate-critical contrast (37 pairs, matched the doc's tightest values), verified every deliverable on disk (that's how the missing route surfaced), and re-validated with the canonical CLI (not the agent's ad-hoc script). The design-delta system surfaced **4 deltas** with correct dispositions (`flag_for_architecture_ADR` ×2, `accept_into_prd`, `park_for_phase_11`) — honoring the P5→P6 silent-divergence guard — plus 2 adversarial-review catches resolved in-spec and 4 assumptions flagged (not silent). | Works — the delta/reconciliation machinery and Butler's don't-trust-verify posture are exactly right. The ✗/⚠ above are gaps *around* a design phase that otherwise ran with real rigor. |
| O30 | 6 Architecture | **✗** | **State drift breaks routing a phase later — `state.yaml` has no write-time validation.** `.coldpress/state.yaml` (the routing source of truth) is validated ONLY on read (`load-state` at SessionStart). In Phase 4/5 Butler improvised non-schema keys — `gates.p5.mandatory_adrs` as a YAML **array** (the gate ledger is scalar-only) and top-level `phase_4/5_completed` (the top level is strict). Nothing caught it until **Phase-6 SessionStart**, where `load-state` failed and **routing broke**. The reconciliation skill doesn't even instruct those keys — Butler invented them, with no guardrail. | **✓ FIXED** — new PostToolUse(Edit\|Write) **`state-validate`** hook validates `.coldpress/state.yaml` against `StateSchema` at write time and feeds errors back in-loop (one edit to fix) instead of surfacing as a next-session routing failure. Registered + wired into `template/.claude/settings.json`; test covers the exact VP drift (array-in-ledger, bespoke top-level key). Butler's own self-repair of the live blocker (re-encode array→string, relocate `phase_N_completed`→`gates.pN.exited`, verified against the real `load-state` hook) was exemplary. |
| O31 | 6 Architecture | ⚠ | **My O28 fix was Phase-4/5 only — Phases 6-11 gates showed the same pattern.** Phase-6's gate auto-passed 2 machine checks and deferred the rest (Butler: "the documented runner pattern"). On closer look this is **largely by design**: the gate model is two-tier — the CLI machine-checks `command`/`artefact-present`; `skill_ref` checks are agent-evaluated (correctly reported `pending`). | **✓ FIXED (surgically) — with a self-correction.** I first over-reached (blanket-flipped every stranded check to a new `agent` kind — broke a gate contract, reverted). Corrected version: converted only the **pure-existence** checks (artefact_path, no skill_ref) in Phases 6-11 to `kind: artefact-present` so the CLI verifies them deterministically; left `skill_ref` checks as the intended agent-evaluated model. Honest note: O28/O31 are *hardening* (shift work from agent-attestation to deterministic CLI checks), not fixes for a broken gate — my original O31 framing overstated it. |
| O32 | 6 Architecture | ○ | **CLI `validate-schema` can't route `.meta.json`/handoff/sidecar files** — the verb uses the unified `validateDocSchema` (basename + path-pattern) but those schemas aren't in its routing registries (the write-time `schema-validate` hook covers them via the design/data-artefact registries). Butler validated them by driving the live hook — a valid workaround. | **Known limitation — not fixed** (same deferred class as O22's prototype-manifest). Candidate: have `validateDocSchema` consult the same registries the hook does (design/data-artefact) so the CLI verb reaches parity. Low priority — write-time coverage + the hook workaround suffice. **✓ FIXED** — the deeper truth: `validateDocSchema` already consults all four registries, but the meta/handoff schemas (`architecture-meta`, `phase-handoff`, `prd-to-architecture`) were **never registered** anywhere (WS1-E deferral), so *nothing* validated them — Butler's "no output = clean" was a misread of "no schema, no opinion." Registered all three (path-pattern for the two JSON sidecars + data-artefact registry for the Zod prd sidecar), taught `loadValidatorByRelPath` to preload the handoff sub-schemas so `phase-handoff`'s `$ref`s resolve, and — a real drift the silence had hidden — widened the NFR-category enum (added ISO-25010 `reliability`/`compatibility`, which the live `prd.meta.json` used). All 3 live meta sidecars now validate via the CLI; new `test/meta-sidecar-routing.test.ts` guards it. Green bar 974; committed 1326ee0, re-vendored da04aab. |
| O33 | 6 Architecture | ⚠ | **The architecture-design ADR authoring didn't instruct the canonical `id`** (only Phase-3's `stack-evaluation` did — O18). The adr schema forced it via schema-validate, but the template didn't guide it. | **✓ FIXED** — step-05-adr template + instruction now carry `id: "ADR-NNNN"` (sequential across Phases 3+6). |
| O34 | 6 Architecture | ⚠ | **NEW — two divergent ADR frontmatter conventions.** Phase-3 `stack-evaluation` ADRs use `{id, name, decision_area, options, chosen, rubric, tier, derived_from}` (matches `adr.schema.json`); Phase-6 `architecture-design` ADRs use a *different* shape `{id, adr_number, title, resolves_design_delta, prd_section_affected, …}` that does **not** satisfy `adr.schema.json`'s required set. So Phase-6 ADRs likely aren't validated against `adr.schema.json` (they "validated clean" because nothing routes them to it), and `validate-adrs` (Phase-3 coverage check) doesn't run on them. | **Logged, not fixed — needs a design decision, not a rushed patch.** Options: (a) a distinct `architecture-adr` schema for the Phase-6 shape + wire it into routing + a Phase-6 `validate-adrs`-equivalent; or (b) unify both ADR types onto one schema. Surfaced while doing O33; reconciling it safely is its own task. **✓ FIXED** — the reconciliation showed it was **not** two conventions in use: both phases' ADRs conform to the single `adr.schema.json` (the write-time hook routes `adr-*-v{N}.md` to it), and the real ADRs @architect landed use the stack shape. It was the Phase-6 *template* that drifted (O12-class) — @architect had to reverse-engineer the conforming shape (forced right by O9). Fix = rewrite the Phase-6 ADR template to the one conforming shape (an architecture decision is still a decision-with-alternatives, so `options/chosen/rubric/tier` fit), keeping `resolves_design_delta` + delta extras (the schema already patterns `resolves_design_delta`). New `test/adr-templates-schema.test.ts` asserts **both** ADR templates carry every schema-required field so they can't drift apart. No schema split needed. Green bar 970; committed f694f56, re-vendored 4161a5a. |

---

## Phase progress (full lane)

- [x] **1 · Bootstrap** — `intake` complete (greenfield; profile=custom; `context.md` authored + sacred-signed-off; working-mode summary/solo/claude-code; deploy=cloudflare, content_workstream flag; `_input/INDEX.md` = 57 cards). **P1 gate passed (0 block failures)**; handoff logged; state advanced 1→2. **6 findings harvested (O1–O7)** — two ✗ (sacred-guard first-create block; intake-template schema drift), three ⚠ (offered Phase-3 skip; profile gap; review-in-chat), one ○ pattern-watch (cwd drift). `next_skill: research`.
- [x] **2 · Discovery** — complete. 2 research passes + synthesis + validate-idea (PROCEED) + `product-brief-v1` (north-star = depth-engagement). **P2 gate passed (0 block failures)**; handoff logged; state 2→3. Clean — **O8** (O1/O2 correctly didn't bite; Discovery is non-sacred).
- [x] **3 · Tech-Stack** — complete. Sacred `tech-stack.md` **locked** (approved_by: user) + 7 ADRs + stack-shortlist + selection-summary + baselines→coldpress.yaml; real Vite+React+TS scaffold builds/lints clean + **smoked locally** through the Cloudflare Pages adapter; license scan clean; escape-hatch red-flags zero-critical. **P3 gate passed (0 block failures)**; state 3→4. **7 findings (O9–O15)** — one ✗ (**O9** Bash-write bypass — authoring agents evade the hook layer), plus O10 (O1 confirmed pre-fix at lock), O11 (no local-first walking-skeleton path), O12 (schema drift systemic), O13 (@architect stalled), O14 (no-pack friction), O15 (strong signals + 47 files uncommitted). Remote deploy honestly **owed to Phase 9**.
- [x] **4 · Planning** — complete. Sacred `prd.md` **locked** (22 FRs = 15 P0 + 7 P1, trimmed from 28 P0; 7 NFR axes) + `outcomes.yaml` (block-gate green) + `prd.meta.json` sidecar + validation record; ADRs got canonical `ADR-0001…0007` ids. **P4 gate green** (only phase-4→5 handoff intentionally held). **6 findings (O16–O21)** — one ✗ (**O16** prd template froze the doc mid-authoring; the O12 audit missed it — now **✓ fixed**), plus O17 (subagent bypass attempts, blocked), O18 (ADR-id gap), O19/O20 (minor), O21 (positive — clean sacred-change unlock held). **Phase 5 held** at user request.
- [x] **5 · Design** — complete. personas/brief/ux-spec/brand-guidelines+tokens/prototype all done & validated; `/styleguide` route wired (build+lint green). **Exit reconciliation** ruled on **5 deltas** (3 → mandatory Phase-6 ADRs, 1 → PRD v1.1 amendment via `CHG-prd-2`, 1 → parked Phase 11). **8 findings (O22–O29)** — three ✗ (**O22** schemas not vendored → **✓ fixed**; **O23** no contrast validator/gate; **O28** Phase-4/5 gates agent-attested not machine-evaluated), O24 (route landed but no gate check), O25 (personas cross-phase gap), O26 (truncation), O27/O29 (positives — verification, delta system, locked-doc amendment path).
- [x] **6 · Architecture** — complete. Opened with a **routing-breaking state blocker** (O30, self-repaired by Butler + now fixed by the `state-validate` hook). `@architect` authored (via Write — O9) sacred `architecture.md` + meta sidecar + **4 ADRs** (the 3 mandatory flagged-delta ADRs → silent-divergence guard satisfied + a privacy-telemetry ADR) + diagrams; `trace orphans --strict` clean; react-router-dom authorised via an ADR-0001 amendment. **P6 gate green.** **5 findings (O30–O34)** — one ✗ (**O30** state write-validation, fixed), O31 (extended machine-checking to Phases 6-11 + self-corrected an over-reach), O32 (CLI schema-routing known-limit), O33 (architecture ADR ids, fixed), **O34 (new — two divergent ADR conventions, logged for a design decision)**. Held at P6→P7.
- [ ] **6 · Architecture** — architecture-design (sacred) · data-model · api-contract · integration-inventory · threat-model · security-registry · analytics-plan. P5∥P6 overlap allowed. `trace orphans` at exit.
- [ ] **7 · Breakdown** — story-slice · story-graph → `coldpress waves` · implementation-readiness. P7 gate.
- [ ] **8 · Implementation** — dev-story / integration-story / quick-dev, then **verify-story** through the **@verifier clean-room** (VP2's required verifier path). At least one **styleguide-driven UI story**. Boundary/git guards live.
- [ ] **9 · Deployment** — readiness · deploy-staging · smoke · deploy-prod (or simulated) · handover. Staging never red.
- [ ] **10 · Operate** — ops-check · operate-loop. (Light for a validation run.)
- [ ] **11 · Evolve** — retrospective · **pack-harvest** · framework-feedback. Harvest ⚠/✗ → `evals/` + this log; update the §12 gate.

---

### Version Control

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0 | 2026-07-07 | Butler (VP2 launch) | Initial VP2 log. Records the subject (Ancient Games IP product, full lane), the deliberate IP-product-subject divergence from VP1's throwaway convention (justified by plan §10), the consumer-session driver (most-faithful, real subagent dispatch), and Phase 0 (pre-overhaul→0.4.0 re-init in place: findings P0-1 no upgrade path / P0-2 clean re-init / P0-3 doctor green). Drive begins at Phase 1 intake. |
| 1.11 | 2026-07-09 | Butler (VP2 O32 fix — last one) | Took on O32 for completeness. Root cause was sharper than logged: `validateDocSchema` already consults all four registries, but the `architecture-meta`/`phase-handoff`/`prd-to-architecture` schemas were **never registered** (WS1-E deferral) — so nothing validated those artifacts, and Butler's "no output = clean" was a misread ("no schema, no opinion"). Registered all three (PATH_PATTERN for the JSON sidecars; data-artefact registry for the Zod prd sidecar), added handoff-sub-schema preloading so `phase-handoff` `$ref`s resolve, and widened the NFR enum (ISO-25010 `reliability`/`compatibility` — a real drift the routing-silence had hidden). New `meta-sidecar-routing.test.ts`. Green bar **974 tests**, drift OK. Commit 1326ee0, re-vendored da04aab. **Every VP2 finding O1–O34 is now fixed.** |
| 1.10 | 2026-07-09 | Butler (VP2 O34 fix) | Took on O34 (ADR-convention divergence). Investigation showed it was an **O12-class template drift, not a two-schema problem**: both phases' ADRs already conform to the single `adr.schema.json` (write-time hook routes `adr-*-v{N}.md`; the real Phase-6 ADRs @architect landed use the stack shape — it reverse-engineered it, forced by O9). Fix = rewrote the Phase-6 `step-05-adr` template to the one conforming shape (kept `resolves_design_delta` + delta extras — the schema already patterns that field), + new `test/adr-templates-schema.test.ts` asserting both ADR templates carry every required field. No schema split. Green bar 970; f694f56, re-vendored 4161a5a. **All VP2 findings O1–O34 are now fixed except O32 (minor CLI-routing known-limitation, workaround valid).** |
| 1.9 | 2026-07-09 | Butler (VP2 Phase-6 harvest + fixes) | Phase 6 (Architecture) complete; opened with a routing-breaking blocker (Butler self-repaired). Harvested O30–O34. **✗ O30 FIXED** — new `state-validate` PostToolUse hook validates `.coldpress/state.yaml` at write time (was read-only at SessionStart → drift broke routing a phase later); registered + wired + tested. **⚠ O31 FIXED (surgical, self-corrected)** — extended O28's machine-checking to Phases 6-11 (pure-existence checks → `artefact-present`); reverted an initial over-reach (a bogus new `agent` kind) after recognizing the two-tier gate model (CLI + agent-evaluated `skill_ref`) is by design; O28/O31 are hardening, not a broken-gate fix. **⚠ O33 FIXED** — architecture-design ADR template/instruction now carry canonical ids. **○ O32** — CLI `validate-schema` routing doesn't reach meta/handoff schemas (write-time hook does; workaround valid) — known limitation, deferred. **⚠ O34 NEW (logged)** — Phase-3 vs Phase-6 ADR frontmatter conventions diverge; Phase-6 ADRs don't satisfy `adr.schema.json` — needs a design decision, not a rushed patch. Green bar: **968 tests**, typecheck/drift/build OK. Commit 4fad136; re-vendored b7a8efc. |
| 1.8 | 2026-07-09 | Butler (VP2 batch fix pass — all remaining findings) | Paused the drive and **closed every remaining actionable finding** in one pass (14 fixes, green bar each). **Prose/guidance:** O3 (SYSTEM: never volunteer a phase skip), O17 (SYSTEM: STOP + surface on a sacred block — use sacred-change, never rm/override), O26 (subagents end with a deliverables manifest), O4 (None/custom profile first-class), O5 (review-in-file), O13 (architect authors incrementally), O20 (create-prd priority-inflation guard). **Code:** O14 (`update --post-phase-3` no-ops on empty `stack_pack`), O19 (`readStdin` timeout + usage hint). **Enforcement:** O18 (adr.schema requires canonical `id` `^ADR-\d{4}$`, assigned at authoring), O11 (walking-skeleton first-class local-first + structured deploy-debt), O24 (Phase-5 styleguide-present warn check), O25 (personas in wiring manifest + Phase-2 warn check). **Feature:** O23 (`coldpress tokens contrast` — WCAG luminance validator, scope-aware pairing, Phase-5 **block** gate; validated live ancient-games tokens at **49 pairs, 0 failures**). Green bar: **962 tests**, lint/drift/build OK. Commits: b98d2af·c7f03cc·c2b986f·781d793 (+ afeb000 O22); re-vendored 13eb303. **All VP2 ✗/⚠ findings now fixed; remaining ○ are positives or pattern-watch (O6 cwd-drift — no repro).** |
| 1.7 | 2026-07-09 | Butler (VP2 Phase-5 close + O22 fix) | Phase 5 complete (exit reconciliation: 5 deltas → 3 Phase-6 ADRs + PRD v1.1 via CHG-prd-2 + 1 parked); Phase 6 entered. Two new findings: **✗ O28** — Phase-4/5 `acceptance_checks` are `kind: "automated"` with only `artefact_path`/`skill_ref` (no `command`), so the runner reports them `pending` → those gates are agent-attested, not machine-evaluated (fix is a design call: relabel `kind: agent` vs. add real commands — flagged for decision); **○ O29** — the locked-doc amendment path (CHG-prd-2, approved→applied) works end-to-end, completing the author→lock→amend lifecycle. **✗ O22 FIXED** — vendored `schemas/` into the consumer bundle (`frameworkDirs`); green bar 952, re-vendored. Remaining batch: O23 (contrast validator — feature), O24 (styleguide-route gate), O25 (personas wiring), O28 (design decision). |
| 1.6 | 2026-07-07 | Butler (VP2 Phase-5 partial harvest) | Phase 5 (Design) in progress — personas/brief/ux-spec/brand-guidelines+tokens done & validated; prototype + /styleguide route remain. Harvested 6 observations (O22–O27), verified against `frameworkDirs`, `src/`, and `lifecycle/5-design/gate.json`. Two **✗**: **O22** — `schemas/` isn't vendored (excluded from `frameworkDirs`) yet skills cite schema *file paths*, so `@ux-designer` hand-rolled a python validator and reached into the framework source repo (recurring since Phase 3); **O23** — no contrast validator anywhere in `src/`, `tokens` has no contrast subcommand, and the Phase-5 gate has no contrast check — the ELEVATED WCAG a11y requirement is neither tooled nor gated (hand-checked by @ux-designer + Butler, 37 pairs). Plus **O24** (live /styleguide route is a stated deliverable with no gate check → silently skipped), **O25** (Phase-2 emitted no personas file though Phase-5 expects one — wiring-manifest gap), **O26** (recurring subagent report truncation), **O27** (positive — Butler's independent re-verification + the 4-delta forward-carry/silent-divergence system working). No fixes applied yet — Phase 5 mid-flight; O22/O23 flagged for a fix decision. |
| 1.5 | 2026-07-07 | Butler (VP2 Phase-4 harvest + O16 fix) | Phase 4 (Planning) complete; harvested 6 observations (O16–O21). Headline **✗ O16**: the `create-prd` finalize template stamped `governance: "requires-review"` at authoring — the only sacred doc born locked — so post-O1 the PRD froze mid-authoring before the 28→15 P0 re-prioritization could run; the O12 audit missed it (checked schema-validity, not governance-vs-flow). **✓ FIXED** — step-05 now authors `governance: "draft"`, flips to `requires-review` only after sign-off (matching the other three docs); green bar (952 tests), committed to framework `main`, re-vendored. Also logged: O17 (subagent tried to `rm`/override the guard — both blocked; enforcement held), O18 (ADR-id gap surfaced two phases late), O19/O20 (minor), O21 (positive — the sacred-change clean-unlock path worked end-to-end). Clarified the O10 story: O1 worked; O16 was the separate template bug it exposed. |
| 1.4 | 2026-07-07 | Butler (VP2 O9/O12 valet-loop) | Fixed the Phase-3 ✗ (O9) + the systemic drift (O12) before Phase 4. **O9**: gave `@architect`+`@analyst` `Edit`/`Write` so their sacred-doc writes route through the enforcement hooks (roster regenerated); this also makes `schema-validate` fire live for them. **O12**: audited all four sacred creation templates — tech-stack (`sacred:false`→`true`, added governance/workflowType, fixed version + moved the lock to the `governance` flip in step-04, resolving an internal validate-before-sacred contradiction) and architecture (unquoted `version: 1.0`→`"1.0"`); context/prd already correct; added `test/sacred-doc-templates-schema.test.ts` round-tripping all four against their schemas. Green bar: typecheck clean, 952 tests (+4), lint OK, `check:drift` OK, build OK. Committed to framework `main` (`1b12fd7`); re-vendored into the devSandbox branch (`90ff3a4`). Also committed the devSandbox Phase 1–3 build to branch `vp2/phases-1-3-build` (`f783d40`). O9/O12 marked ✓ FIXED. |
| 1.3 | 2026-07-07 | Butler (VP2 Phase-2/3 harvest) | Phases 2 (Discovery) + 3 (Tech-Stack) complete; harvested 8 observations (O8–O15) verified against roster + settings.json + landed devSandbox artifacts. Headline **✗ O9**: authoring agents (`@architect`, `@analyst`) have Bash-but-no-Write toolsets, so their sacred/schema'd writes bypass all five `Edit\|Write`-scoped hooks (sacred-guard, boundary-guard, secret-scan, schema-validate, test-integrity) — the sacred-doc protection is evadable by the agents that author sacred docs. **O10**: O1 confirmed in the wild (tech-stack.md lock needed a pre-fix override; passes-after to verify at Phase 4). **O11**: no first-class local-first walking-skeleton path (deferring remote deploy forced an override on a ★ block gate; user-flagged). **O12**: template↔schema drift systemic (agents reverse-engineered ADR/shortlist/summary/tech-stack/baselines schemas; compounded by O9). **O13**: @architect stalled before writing (82k-token read, resumed by Butler). **O14**: no-pack/custom-profile friction. **O15** (positive): real quantitative escape-hatch thresholds, genuine license scan, honest evidence-backed overrides — discipline held at Phase-3 scale; 47 files uncommitted (process note). O9 recommended for valet-loop before Phase 4. |
| 1.2 | 2026-07-07 | Butler (VP2 O1/O2 valet-loop) | Fixed the two ✗ findings before resuming the drive. **O1**: `sacred-guard` pre-lock exemption (allow creation + `governance: draft`; protect only `locked`/`requires-review`) + corrected the behavior-enshrining test + new create/draft/locked/`isLockedOnDisk` cases. **O2**: intake seed template made schema-valid (`sacred`/`governance: draft`/`workflowType`), step-11 promotes `governance → locked` at signoff, new round-trip test validates the actual template through the real validator. Green bar: typecheck clean, 948 tests (+8), lint OK, `check:drift` OK, build OK. Committed to framework `main`; re-vendored the 4 template files into the devSandbox (O1 ships via the on-PATH CLI). O1/O2 dispositions marked ✓ FIXED. Follow-up logged: generalize the seed-template round-trip to prd/tech-stack/architecture. |
| 1.1 | 2026-07-07 | Butler (VP2 Phase-1 harvest) | Phase 1 intake complete; harvested 7 observations (O1–O7) verified against framework source. Two **✗** confirmed bugs bite every full-lane intake: **O1** `sacred-guard` blocks the designed first-creation of `context.md` (no create/seed exemption); **O2** intake seed template omits 3 of 4 schema-required frontmatter fields → `context-schema-valid` fails on a straight-from-template doc (O8-class drift; exposes a systemic gap — no test asserts skill-emitted sacred templates satisfy their schemas). Three **⚠**: **O3** Butler's guidance offered a Phase-3 (Tech-Stack) skip that the `phase-gate` hook *would* deny — guidance↔enforcement disagreement; **O4** profile roster gap for content-heavy client-side interactive apps (echoes VP1 O5); **O5** review-in-chat vs review-in-file ergonomics. One **○** pattern-watch (O6 cwd drift) and one positive **○** (O7 — enforcement machinery demonstrably firing in a real consumer session). O1 + O2 recommended for valet-loop fix before the drive continues (they recur). Phase-1 progress box checked. |
